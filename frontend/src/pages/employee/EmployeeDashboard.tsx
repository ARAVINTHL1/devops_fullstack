// Employee Dashboard - Uses the same live API-backed dashboard as admin
import AdminDashboard from "@/pages/admin/AdminDashboard";

const EmployeeDashboard = () => <AdminDashboard hideTotalRevenue />;

export default EmployeeDashboard;
